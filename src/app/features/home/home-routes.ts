import {Routes} from '@angular/router';
import {HomeRouter} from './home-router/home-router.component';

export const HOME_ROUTES: Routes = [
    {
        path: '',
        component: HomeRouter
    }
];
