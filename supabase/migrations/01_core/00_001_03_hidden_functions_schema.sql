-- Creates a "hidden_functions" schema which is not accessible to the client API
-- Thus, these functions are protected
-- and it is ensured that they can only be called by transactions
--
-- CREATE SCHEMA IF NOT EXISTS hidden_functions;
-- GRANT ALL ON SCHEMA public TO postgres;
-- GRANT ALL ON SCHEMA hidden_functions TO postgres;

